{
  "name": "gif-search",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://kautilyak.github.io/gif-search",
  "devDependencies": {
    "react-scripts": "0.8.5"
  },
  "dependencies": {
    "gh-pages": "^2.2.0",
    "react": "^16.12.0",
    "react-dom": "^16.12.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  }
}
